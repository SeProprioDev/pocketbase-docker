  apiVersion: v1
kind: Service
metadata:
  name: {{ include "pocketbase-helm.fullname" . }}
  labels:
    {{- include "pocketbase-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pocketbase-helm.selectorLabels" . | nindent 4 }}
